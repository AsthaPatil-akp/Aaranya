[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback. Do not proxy /api here — the FastAPI backend is hosted elsewhere.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
