# Copy to frontend/.env only if you need to override the Vite proxy.
# Local `npm run dev`: leave empty so /api is proxied to the local FastAPI process.
# Production / Netlify: set this in the Netlify UI (never commit a real backend URL).

VITE_API_URL=https://YOUR-BACKEND-URL
